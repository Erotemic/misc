from setuptools import setup


if __name__ == '__main__':
    setup(
        name='dummy_pkg2',
        version='1.0.0',
        install_requires=[
            # 'opencv-python == 4.5.1.48'
        ],
        packages=['dummy_pkg2'],
    )
